# .cursor/docs

Optional folder for long-form context the agent can consult on demand
(architecture notes, ADRs, vendor API references, etc.).

Add markdown files here and reference them from rules or commands when needed.
