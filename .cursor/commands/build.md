# /build

Build the `aws-sso-login` binary for the current host.

Steps:

1. Run `make build`.
2. Confirm the binary exists at `./tmp/aws-sso-login`.
3. Optionally print `./tmp/aws-sso-login --help` to verify it runs.
