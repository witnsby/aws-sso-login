# /test

Run the Go test suite for this repository.

Steps:

1. Run `make tests`.
2. If failures occur, surface the failing package and test name.
3. Suggest a minimal fix; do not modify unrelated tests.
